export interface Vino {
  id: number;
  name: string;
  bodega: string;
  region: string;
  notes: string;
  pairing: string;
  year: string;
}

export interface Reserva {
  name: string;
  email: string;
  phone: string;
  pax: string;
}
